import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.precisionlighting.app',
  appName: 'Precision Lighting',
  webDir: 'out',
};

export default config;